import 'package:flutter/material.dart';

class NotificationEx extends StatefulWidget {
  const NotificationEx({super.key});

  @override
  State<NotificationEx> createState() => _NotificationExState();
}

class _NotificationExState extends State<NotificationEx> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("data"),
    );
  }
}
